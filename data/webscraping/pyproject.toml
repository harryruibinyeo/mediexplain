[project]
name = "webscraping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "browserforge>=1.2.4",
    "curl-cffi>=0.15.0",
    "playwright==1.59.0",
    "scrapling[fetchers]>=0.4.8",
    "structlog>=25.5.0",
]
