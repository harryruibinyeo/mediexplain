[project]
name = "mediexplain-ui"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gradio>=5.0",
    "httpx>=0.28",
    "structlog>=24.0",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
