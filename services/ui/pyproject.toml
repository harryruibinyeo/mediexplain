[project]
name = "mediexplain-ui"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40",
    "httpx>=0.28",
    "plotly>=5.0",       # charts for monitoring tab
    "structlog>=24.0",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
